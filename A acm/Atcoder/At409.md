# [B - Citation](https://atcoder.jp/contests/abc409/tasks/abc409_b)
```cpp
#include<bits/stdc++.h>
using namespace std;
#define endl "\n"
//#define int long long
#define ll long long
const int N = 3e2+7,inf=0x3f3f3f3f;
void solve(){
    int n,mx=0; cin>>n;
    vector<int>a(n+1);
    for(int i=1;i<=n;++i)cin>>a[i];
   sort(a.begin()+1,a.begin()+1+n,greater<int>());
   for(int i=n;i;--i){
    if(a[i]>=i){cout<<i<<endl; return ;}//比a[i]大的大于等于i个
   }
   cout<<0<<endl;
}
signed main(){
    ios::sync_with_stdio(0),cin.tie(0),cout.tie(0);
    int T=1;
    //cin>>T;
    while(T--)solve();
    return 0;
}


```