```cpp
如果要再某一字符串中找寻(或对比)字串，可以先把字串设为一个数组

strstr用于在一个字符串中查找子字符串的第一次出现位置。返回类型时指针

stable_sort能保证原相等元素的相对位置保持不变

unorder_map: 无序map

multimap: 升序可重复map

underored_multimap:无序可重复map

乘法太大溢出之后可能会出现负数影响判断（因为会直接截断），所以最好不要有溢出，
解决办法是将乘法转化为除法

cin.peek() 可以用来查看输入流中的下一个字符，但不将其从流中提取
可以用来只读入两个整数

即 while(cin>>a&&cin.peek()!='\n') 相当于while(scanf("%d%d",&a,&b)==2);

dfs用于邻接表时时间复杂度时O(E)(顶点个数)

for(int i=0;i>0;--i) 如果第一个不满足第二个的条件，for循环会直接不执行

大根堆大的在上，小根堆小的在上

1e-6 即 10的-6次方

链式前向星重置方式：(需要重置h和idx)
memset(h,-1,sizeof h);
idx=0;

二维vector: vii g(n + 1, vi(m + 1));

简单无向图没有重边和自环
```
